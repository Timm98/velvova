# 3D-Modelle

Dateien hier werden unter `/models/<name>` ausgeliefert.

Beispiel: `apps/web/public/models/nina.glb` → `http://localhost:3000/models/nina.glb`

Das ist das öffentliche Verzeichnis der Web-App. Ein `public/` im
Projektstamm würde von Next.js nicht ausgeliefert.
